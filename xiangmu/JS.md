Javascript

```javascript
window.onload = function() {
    //隐式推导：通过原始值推导出变量类型
}  //类似Java里mian方法，是主方法/主函数
function fun1(){}
function fun2(obj){
    return ;
}
```

```javascript
alert("");/相当于Java system.out.print("");
var 变量名 = 赋的值;/定义变量
let 常量名 = 赋的值;  /定义常量
var 数组 = [15，45，2，36，78，2];  /定义数组
数组名.push();/向数组添加数据，默认最后一位
数组名.pop();/删除数组最后一个数据
数组名.shift();/删除第一个数据
数组名.splice(0，1,100);/从第0位开始，删除1个,添加一个100 /修改功能
alert(数组名[下标]);/指定打印某个数组数据 /查询功能
var 元组名 = {"str":"hello","str2":"world"};/定义元组
/*例：var num = 100;
	 var str = " ";*/
sign :自定义标记 在循环中使用跟this关键字一起使用
document 
paresInt()
removeChild(num):移除某个标签
```

```javascript
function produseJustice(count){
	//0-9,a-z,A-Z
}
```

```javascript
var oDiv = document.getElementById("")      //获取所有权
oDiv
```

```
onload 加载完成事件： 页面加载完成之后，常用于做页面 js 代码初始化操作
onclick 单击事件： 常用于按钮的点击响应操作
onblur 失去焦点事件： 常用用于输入框失去焦点后验证其输入内容是否合法
onfocus 聚焦事件：常用用于输入框焦点后验证结果消除
onchange 内容发生改变事件： 常用于下拉列表和输入框内容发生改变后操作
onsubmit 表单提交事件： 常用于表单提交前，验证所有表单项是否合法
onmousemove 事件会在鼠标指针移到指定的对象时发生。
onkeydown 侦测键盘事件
```

```javascript
window.onload = function(){
    var oDiv = document.get
}
```

```
mybatrs  spring

```



